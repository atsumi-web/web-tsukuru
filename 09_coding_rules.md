# Antigravity コーディング・ガイダンス (CSS編)

このプロジェクトにおいて、AI（Antigravity）がデザイン修正や機能追加を行う際に厳守すべきルールを定めます。

## 1. CSS構造の原則

- **シングル・ソース・オブ・トゥルース**: すべてのスタイルは原則として `css/renewal.css` に集約する。
- **コンポーネント指向**: クラス定義のすぐ直下に、そのクラスに関するメディアクエリ（@media）を記述する。
- **!important の禁止**: 原則として `!important` は使用しない。詳細度の問題は、`:where()` セレクタによるリセット、またはHTML構造の見直しで解決する。

## 2. デザイン・トークンの使用

- **変数の徹底活用**: 色、数値（padding, gap）、フォント名はすべて `:root` で定義された変数を使用する。
- **ハードコードの禁止**: `#ffffff` や `12px` などの値を直接各セレクタに記述しない。必要に応じて `:root` に新しい変数を定義してから使用する。
- **動的パディング**: セクションの上下余白など、画面幅に依存する数値は `clamp()` 関数を使用して滑らかに可変させる。

## 3. 命名規則と状態の管理

- **命名の一貫性**: 新しいクラス名を作成する際は、既存の命名規則（`.cta-button`, `.price-card` 等）を逸脱しない。
- **ステート（状態）のセット記述**: 要素の色やスタイルを変更する場合、必ず `:hover`, `:active`, `::before/after` などの関連する状態もセットで確認・修正する。
- **セルフ・クリーンアップ**: HTMLから要素が削除された場合、対応する不要になったCSSセレクタも速やかに削除候補として提案または削除する。

## 4. コードの物理的管理

- **追記ではなく「上書き」**: 新しいスタイルが必要な場合、既存のクラス定義を探して直接編集する。ファイルの末尾に新しいルールを追記しないこと。
- **意味のあるコメント**: セクションの区切りや、複雑なアニメーションの意図を日本語で明記する。
- **UTF-8保存の徹底**: ファイルの保存時は必ず `UTF-8` エンコーディングを使用し、日本語コメントの文字化け（Mojibake）を絶対に起こさない。

## 5. AIへの指示・作業フロー

ユーザーからデザイン修正の依頼があった際、AIは以下のステップで実行する。

1.- **「09_coding_rules.md を厳守し、既存の構造を壊さない最小限かつメンテナンス性の高い美しいコードで出力してください。その上で、今の修正案の中で最も成約率（応募数）に直結する変更から優先的に実行してください。」** 2. **最小限のコード変更で目的を達成する（DRY原則）。** 3. **修正完了後、コード全体のインデントや整理が保たれているかセルフチェックする。**

---

_最終更新日: 2026-01-29 (Ver 3.2.0)_
